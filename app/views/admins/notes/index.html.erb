<div class="container">
  <div class="row">    
    <table class="table table-hover">
      <thead>
        <th>タイトル</th>
        <th>タグ</th>
        <th>投稿者</th>
        <th></th>
      </thead>
      <tbody>
        <% @notes.each do |note| %>
          <tr>
            <td style="width:200px;">
              <%= note.title %>
            </td>
            <td>
              <% note.tag_list.each do |tag| %> 
                <span class="badge-primary p-1 badge-tag"><%= tag %></span>
              <% end %>
            </td>
            <td>
              <%= note.user.name %>
            </td>
            <td><%= link_to "詳細",admins_note_path(note),class: "btn btn-primary" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>