<div class="container">
  <div class="row">    
    <table class="table table-hover">
      <thead>
        <th>ユーザ名</th>
        <th>メールアドレス</th>
        <th>自己紹介</th>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
              <td style="width:200px;"><%= link_to user.name, admins_user_path(user)%></td>
              <td style="width:400px;"><%= user.email %></td>
              <td><%= user.introduction %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
